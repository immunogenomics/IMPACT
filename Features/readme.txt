gzipped bed files for all features used in IMPACT where the directory name indicates the cell type of the features. 

Supplementary Table 1 has the key that corresponds number.bed to feature type and GEO accession.
Note, files do not have unique integer values because they are stored in different cell-type-specific directories. 

